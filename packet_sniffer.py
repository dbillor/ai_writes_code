fom scapy.all impot *\nimpot collections\nimpot time\n\ndef packet_callback(packet):\n    if IP in packet:\n        ip_sc = packet[IP].sc\n        ip_dst = packet[IP].dst\n        ip_counte.update([ip_sc, ip_dst])\n\nif __name__ == __main__:\n    ip_counte = collections.Counte()\n    while Tue:\n        sniff(pn=packet_callback, count=100)\n        top_ips = ip_counte.most_common(10)\n        pint(Top10IPs:)\n        fo ip, count in top_ips:\n            pint(f{ip}:{count})\n        time.sleep(60)
